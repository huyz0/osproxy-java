# Reference configuration; every key is overridable via config sources
# (e.g. environment: OSPROXY_<NAME>, e.g. OSPROXY_TENANT_METRICS_ENABLED for
# tenant-metrics-enabled below — see ProxyConfig.ENV_ALIASES for the full
# list; Main boots via ProxyConfig.createConfig(), not Config.create()).
# See osproxy-config's ProxyConfig.
osproxy:
  port: 9201
  upstream: "http://localhost:9200"
  index: "shared"
  # tokens:
  #   some-secret-token: "acme"
  # cursor-affinity-key: "some-long-random-key"
  # directive-admin-token: "some-admin-token"
  # otlp-endpoint: "http://localhost:4318"
  # directives-url: "http://config-service/osproxy/directives"
  # fips: true
  # placements-url: "http://config-service/osproxy/placements"
  # fanout:
  #   bootstrap-servers: "localhost:9092"
  #   topic: "osproxy-writes"
  # tenant-metrics-enabled: true
  # tenant-metrics-export-interval-seconds: 15
